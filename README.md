# Bug

Do `yarn svelte:build`
Then enter the dist/www folder and open a index.html file inside one of the routes folders.

Open it in the browser and the file will not work because the links are wrongly configured
